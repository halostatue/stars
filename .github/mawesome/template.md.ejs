# @<%= username %>'s Stars

<% const myStars = stars.reduce((a, [g, r], ) => a.set(g, r), new Map()) %>
<% const keys = Array.from(myStars.keys()).sort() %>

> :star: generated with [simonecorsi/mawesome](https://github.com/simonecorsi/mawesome)

## Table of Contents

<% for (let grouping of keys) { %>
## <%= grouping %>

<% const repos = myStars.get(grouping) %>
<% for (let repo of repos) {%>- [<%= repo.full_name %>](<%= repo.html_url %>) - <%= repo.description %>
<% } %>
<% } %>
